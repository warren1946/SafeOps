meta {
  name: List Safety Events
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/safety/events
  body: none
  auth: bearer
}

headers {
  X-Tenant-ID: {{tenantId}}
  X-Tenant-Slug: {{tenantSlug}}
}

assert {
  res.status: eq 200
}

docs {
  # List Safety Events
  
  ## Expected Response
  - Status: 200 OK
  - Returns list of safety events
}
